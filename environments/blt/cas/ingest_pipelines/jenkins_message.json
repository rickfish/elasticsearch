{ 
	"description": "Pipeline for parsing jenkins message log",
	"processors": [
		{
			"grok": {
				"ignore_missing": true,
				"field": "message",
				"patterns": [
				"""finish: %{DATA:JOB_NAME} %{DATA:BUILD_NUMBER}\s*\b\w+\b:\s*%{DATA:JOB_STATUS}$"""
				]
			}
		}
	]
} 
